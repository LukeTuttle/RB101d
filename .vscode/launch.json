{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Current File",
      "type": "Ruby",
      "request": "launch",
      "program": "${file}",
      "cwd" : "${workspaceRoot}"
    }
  ]
}